import styled from 'styled-components';

const BoardHeaderMenuBox = styled.button`
    width: 40px;
    height: 40px;
    margin-right: -10px;
    background: none;
    border: none;

    justify-content: center;
    align-items: center;

    cursor: pointer;
    :focus{
        outline: none;
    }
`;

export default BoardHeaderMenuBox;